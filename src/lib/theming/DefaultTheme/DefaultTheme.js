// TYPOGRAPHY
const mainFontFamily = 'Roboto';
const secondaryFontFamily = 'Roboto Slab';

const smallFontSize = 'clamp(10px, 1.2vh, 14px)';
const defaultFontSize = 'clamp(12px, 1.5vh, 18px)';
const sectionTitleFontSize = `calc( 1.1 * ${defaultFontSize})`;

const primaryFontColor = '#3b3b3b';
const secondaryFontColor = 'white';

// COLORS
const colorDanger = '#f86c72';

const DEFAULT_THEME = {
  // TYPOGRAPHY
  mainFontFamily,
  secondaryFontFamily,
  smallFontSize,
  defaultFontSize,
  sectionTitleFontSize,
  // COLORS
  primaryFontColor,
  secondaryFontColor,
  colorDanger,
};

export default DEFAULT_THEME;
