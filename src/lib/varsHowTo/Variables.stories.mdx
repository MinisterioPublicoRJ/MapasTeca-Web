import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="CSS / Variáveis" />

# Nomes das variáveis e valores default

## Tipografia
mainFontFamily: Roboto;

secondaryFontFamily: Roboto Slab;

smallFontSize: clamp(10px, 1.2vh, 14px);

defaultFontSize: clamp(12px, 1.5vh, 18px);

sectionTitleFontSize: calc( 1.1 * var(--defaultFontSize));

## Cores
primaryFontColor: #3b3b3b;

secondaryFontColor: white;

primaryLinkColor: #28A7E0;

colorDanger: #f86c72;

primaryColor: #BDDFF0;
